<section class="contact section">
    <div class="container contact__container">
      <div class="contact__img">
        <img src="/resource/img/contact.svg" alt="contact" />
      </div>
      <div class="contact__form-wrapper">
        <h3 class="contact__form-title">Contáctanos</h3>
        <p class="contact__form-subtitle">En Sofdata, estamos aquí para ayudarlo a aprovechar al máximo sus datos. Ofrecemos una amplia gama de servicios de análisis de datos y big data para ayudarlo a tomar decisiones más inteligentes, mejorar su eficiencia y aumentar sus ganancias.</p>
        <form class="form">
          <input class="input form__input" type="text" placeholder="Tu nombre completo" />
          <input class="input form__input" type="text" placeholder="Tu correo electrónico" />
          <textarea class="textarea form__textarea" placeholder="Tu mensaje"></textarea>
          <a href="" class="button">Enviar Mensaje</a>
        </form>
      </div>
    </div>
  </section>
<style>
.contact {
  padding: 5rem 0; /* Adjust padding as needed */
  background-color: var(--secondary-color); /* Optional background color */
  display: flex;
  flex-direction: column;
  align-items: center; /* Center elements horizontally */
  text-align: center; /* Center text for smaller screens */
  max-width: 80%; /* Set maximum width for the contact section */
  margin: 0 auto; /* Center the section horizontally */
}

.contact__container {
  display: flex;
  flex-wrap: wrap; /* Allow elements to wrap on smaller screens */
  justify-content: space-between; /* Space elements evenly */
  width: 100%; /* Elements within the container fill the available space */
  margin: 0 auto; /* Center container horizontally within the .contact section */
}

.contact__img {
  flex: 0 0 35%; /* Set image width */
  margin: 0 auto; /* Center image horizontally and vertically */
  order: 1; /* Move image above form on mobile */
}

.contact__img img {
  width: 100%; /* Image fills container */
  height: auto; /* Maintain aspect ratio */
}

.contact__form-wrapper {
  flex: 1 1 auto; /* Allow form to expand and fill available space */
  order: 2; /* Move form below image on mobile */
}

.contact__form-title,
.contact__form-subtitle {
  margin-bottom: 1rem;
}

.form {
  display: flex;
  flex-direction: column;
}

.input,
.textarea {
  padding: 1rem;
  margin-bottom: 1rem;
  border: 1px solid var(--border-color);
  border-radius: 5px;
  width: 100%; /* Inputs take up full width within the form */
  font-size: 1rem;
}

.textarea {
  resize: vertical; /* Allow textarea resizing */
  min-height: 100px; /* Set minimum height for textarea */
}

.button {
  display: inline-block;
  background-color: var(--first-color);
  color: var(--white-color);
  font-weight: var(--font-semi-bold);
  padding: 1rem 1.5rem;
  transition: box-shadow 0.4s;
  border-radius: 10px;
}

.button:hover {
  box-shadow: 0 4px 32px hsla(230, 72%, 32%, 0.4);
}
/* Media query for desktop responsiveness */
@media (min-width: 768px) {
  .contact__img {
    flex: 0 0 35%; /* Increase image width on desktop */
  }
}
</style>